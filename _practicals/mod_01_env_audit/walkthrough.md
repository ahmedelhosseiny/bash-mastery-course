# Mod 1 Practical Walkthrough: The Environment Audit

In this session, we learn how to audit the shell environment and find out which configurations are set.

## Instructions
1. Run the starter template `starter_script.sh`.
2. Inspect variables like `$BASH_VERSION`, `$USER`, and `$HOME`.
3. Use the solution file `solution.sh` to output structured environment details.
