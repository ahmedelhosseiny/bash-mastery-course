# Mod 3 Practical Walkthrough: The Dynamic Configuration Loader

Learn to parse files into associative arrays and use parameter expansions.

## Exercises
- Declare associative arrays using `declare -A`.
- Print keys using `${!array[@]}`.
