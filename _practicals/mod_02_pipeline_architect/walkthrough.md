# Mod 2 Practical Walkthrough: The Pipeline Architect

This practical covers utilizing shell pipelines (`|`), background processes (`&`), and logical separators (`&&`, `||`).

## Exercises
- Use logical chaining to run steps sequentially.
- Pipe a stream through filters like `awk`, `sort`, and `uniq`.
