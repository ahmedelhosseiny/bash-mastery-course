# Exercise 3: Shift Argument Processor
Create a script `shift_arguments.sh` that accepts an arbitrary list of parameters, 
prints the first parameter, shifts, and repeats until all arguments are printed.
