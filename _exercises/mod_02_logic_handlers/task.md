# Exercise 2: Logic Handlers without 'if'
Create a script `logical_run.sh` that checks if a directory named `workspace` exists. 
If it does NOT exist, create it. If it successfully creates it, write "Workspace Created" to stdout.
Use only `&&` and `||` (no if statements).
