# Exercise 1: Custom .bashrc Configuration
1. Write a custom configuration file called `custom_bashrc`.
2. Add aliases for `la` (list all files long-format), and `rm` (interactive remove).
3. Set your PS1 prompt to show `[BASH-MASTERY] \u@\h:\w$ `.
